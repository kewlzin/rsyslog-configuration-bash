# rsyslog-configuration-bash
simple bash script made to suit my needs
it checks if you already have rsyslod on your linux machine and put a configuration file to send the logs to a remote graylog server
